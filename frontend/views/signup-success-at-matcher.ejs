<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="css/signup.css">
    <title><%- title %></title>
    <style>
        body {
            background: #f7f9fb;
            font-family: 'Segoe UI', Arial, sans-serif;
            margin: 0;
            padding: 0;
        }
        .logo {
            text-align: center;
            margin-top: 40px;
            margin-bottom: 10px;
        }
        .container {
            display: flex;
            justify-content: center;
            align-items: flex-start;
            min-height: 80vh;
        }
        .success-container {
            background: #fff;
            box-shadow: 0 4px 24px rgba(0,0,0,0.07), 0 1.5px 4px rgba(0,0,0,0.03);
            border-radius: 18px;
            text-align: center;
            padding: 48px 32px 36px 32px;
            max-width: 400px;
            width: 100%;
            margin: 40px 0;
        }
        .success-icon {
            width: 72px;
            height: 72px;
            background: linear-gradient(135deg, #4CAF50 70%, #43a047 100%);
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            margin: 0 auto 24px;
            box-shadow: 0 2px 8px rgba(76,175,80,0.12);
        }
        .success-icon svg {
            width: 38px;
            height: 38px;
            fill: #fff;
        }
        .success-message {
            color: #222;
            font-size: 1.7rem;
            font-weight: 600;
            margin-bottom: 10px;
            letter-spacing: 0.5px;
        }
        .success-details {
            color: #666;
            font-size: 1.05rem;
            margin-bottom: 32px;
            line-height: 1.6;
        }
        .btn-container {
            display: flex;
            gap: 14px;
            justify-content: center;
            flex-wrap: wrap;
        }
        .btn {
            padding: 12px 28px;
            background: linear-gradient(90deg, #007bff 70%, #0056b3 100%);
            color: #fff;
            text-decoration: none;
            border-radius: 6px;
            border: none;
            cursor: pointer;
            font-size: 1rem;
            font-weight: 500;
            box-shadow: 0 2px 8px rgba(0,123,255,0.08);
            transition: background 0.2s, box-shadow 0.2s;
        }
        .btn:hover {
            background: linear-gradient(90deg, #0056b3 70%, #007bff 100%);
            box-shadow: 0 4px 16px rgba(0,123,255,0.13);
        }
        .btn-secondary {
            background: #f1f3f6;
            color: #333;
            box-shadow: none;
        }
        .btn-secondary:hover {
            background: #e2e6ea;
            color: #222;
        }
        @media (max-width: 600px) {
            .success-container {
                padding: 32px 10px 24px 10px;
                max-width: 98vw;
            }
            .logo {
                margin-top: 24px;
            }
        }
    </style>
</head>
<body>
    <div class="logo"><%- logo %></div>
    <div class="container">
        <div class="success-container">
            <div class="success-icon">
                <svg viewBox="0 0 24 24">
                    <path d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"/>
                </svg>
            </div>
            <h1 class="success-message">Registration Successful!</h1>
            <p class="success-details">
                <%- message %>
            </p>
            <div class="btn-container">
                <a href="/sign-in-at-matcher" class="btn">Sign In Now</a>
                <a href="/main-matcher" class="btn btn-secondary">Back to Scent Matcher</a>
            </div>
        </div>
    </div>

    <script>
        // Auto redirect after 10 seconds
        setTimeout(function() {
            const countdown = document.createElement('p');
            countdown.style.color = '#666';
            countdown.style.fontSize = '14px';
            countdown.style.marginTop = '20px';
            document.querySelector('.success-container').appendChild(countdown);
            
            let seconds = 10;
            const timer = setInterval(function() {
                countdown.textContent = `Redirecting to sign in page in ${seconds} seconds...`;
                seconds--;
                
                if (seconds < 0) {
                    clearInterval(timer);
                    window.location.href = '/sign-in-at-matcher';
                }
            }, 1000);
        }, 3000);
    </script>
</body>
</html>